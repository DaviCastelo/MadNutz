import { HeaderLogo } from "@/components/brand/HeaderLogo";

export function NavLogo() {
  return (
    <a href="#top" aria-label="MadNutz — início" className="inline-flex shrink-0 items-center">
      <HeaderLogo />
    </a>
  );
}
